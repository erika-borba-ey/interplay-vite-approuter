{
  "name": "interplay-vite-approuter",
  "version": "1.0.0",
  "description": "Demonstrate how approuter can work together with Vite",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all -r --parallel start:router start:ui",
    "start:router": "npm --prefix ./router run start:dev",
    "start:ui": "npm --prefix ./ui run dev",
    "setup": "npm install && npm --prefix ./ui install && npm --prefix ./router install"
  },
  "author": "Julian Schambeck",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@sap-cloud-sdk/http-client": "^3.16.0",
    "@sap/xssec": "^4.1.3",
    "passport": "^0.7.0"
  }
}
